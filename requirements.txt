# 视频下载器 —— 运行期只需要这两个 Python 包.
#
# **yt-dlp 不在这里** —— 它以官方 standalone 二进制的形式外挂在
# vendor/ytdlp/yt-dlp.exe, 由子进程调用 (见 app/infra/ytdlp_bin.py)。
#
# 为什么不当库用: PyInstaller 会把纯 Python 库编译进 exe 内嵌的 PYZ 归档,
# 既没有 site-packages 可以 pip 升级, 外部副本也抢不过 FrozenImporter ——
# 等于冻死在打包那一刻。而 YouTube 每隔几周换一次播放器, 旧版 yt-dlp 立刻全线
# 403 (2026-08-21 就这么翻车过)。外挂二进制则可以自更新, 也不受本程序
# Python 3.11 的版本限制。

PyQt6>=6.6.0
loguru>=0.7.0

pyinstaller>=6.0.0          # 仅打包时
